<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/any_idea_root"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <ImageButton
            android:id="@+id/attach_image"
            android:src="@color/cardview_dark_background"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:minHeight="48dp"
            android:minWidth="48dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    <Button
            android:id="@+id/send_message"
            android:text="Senden"
            app:layout_constraintTop_toTopOf="@id/attach_image"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    <EditText
            android:hint="Idee senden"
            android:id="@+id/input_message"
            android:lines="1"
            android:maxLines="1"
            app:layout_constraintEnd_toStartOf="@id/send_message"
            app:layout_constraintTop_toTopOf="@id/attach_image"
            app:layout_constraintBottom_toBottomOf="@id/attach_image"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@+id/attach_image"/>

    <include
            android:id="@+id/toolbar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            layout="@layout/fake_toolbar"/>

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/idea_messages"
            app:layout_constraintVertical_bias="0"
            app:layout_constraintTop_toBottomOf="@id/toolbar"
            app:layout_constraintBottom_toTopOf="@id/attach_image"
            app:stackFromEnd="true"
            app:reverseLayout="true"
            tools:itemCount="4"
            tools:listitem="@layout/any_idea_item"
            android:layout_width="match_parent"
            android:layout_height="0dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>